%% this is file `jluthesis.sty',
%%
%% jlu thesis style package by Zhang Yinhe
%% this is the first package I are trying to write, so some comments may be verbose.
%%
\NeedsTeXFormat{LaTeX2e}[2003/12/01]
\def\fileversion{v0.1}
\def\filedate{2008/08/01}
\ProvidesPackage{jlumastersthesis}
        [\filedate\space\fileversion\space
        style definitions for Master's thesis writing
        conforming to Jilin University's clauses]

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%% NB: 正式打印时，设定\jludebugfalse
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% 测试调试开关，正式打印时，注释掉即可，文档内搜索``测试用''查找测试内容
\newif\ifjludebug               % 默认为false, \jludebugfalse
\DeclareOption{jludebug}{\jludebugtrue\jluelectronicfalse\jlufinalfalse}
\newif\ifjluelectronic
\DeclareOption{jluelectronic}{\jluelectronictrue\jludebugfalse\jlufinalfalse}
\newif\ifjlufinal
\DeclareOption{jlufinal}{\jlufinaltrue\jludebugfalse\jluelectronicfalse}

\newif\ifpdftex
\DeclareOption{pdftex}{\pdftextrue\dvipdfmfalse\dvifalse}
\newif\ifdvipdfm
\DeclareOption{dvipdfm}{\dvipdfmtrue\pdftexfalse\dvifalse}
\newif\ifdvi
\DeclareOption{dvi}{\dvitrue\pdftexfalse\dvipdfmfalse}

\newif\ifchinesepunct                       % 是否使用非等宽(调整後的)中文标点，默认不使用
\DeclareOption{chinesepunct}{\chinesepuncttrue}
\newif\iflinux
\DeclareOption{linux}{\linuxtrue}

% pass it to package `geometry' for page layout setup, if we don't know a option
\DeclareOption*{\PassOptionsToPackage{\CurrentOption}{geometry}}

\ExecuteOptions{jlufinal,dvipdfm}           % 默认选项，jlufinal，dvipdfm

\ProcessOptions

%\RequirePackage{etex}  %增加计数器总数（原来是256，宏包多，可能不够用），编译需基于eTeX

%%% 页面设置
% 版面控制宏包，定义规定的版面尺寸，需要完整阅读geometry宏包的文档
% 设定结果参考log 文件
% NB: 1.    当纸张为非LaTeX/TeX标准纸张时，geometry 宏包的driver必须设定，
%           此外，此时只可使用pdftex 和dvipdfm 驱动来输出pdf 文件，
%           dvips 驱动在剪裁纸张时会出现错误(原因我还不明白)

\RequirePackage[%
    paperwidth=18.4cm,paperheight=26.0cm,   % 16开纸，18.4cm X 26.0cm
    textwidth=14.5cm,textheight=21.0cm,     % 论文版芯145mm X 210mm（包括页眉及页码则为145mm×230mm）
    bindingoffset=0.0cm,                    % 装订线宽度
    inner=1.95cm,                           % 内侧空白（左页边距）
    hmarginratio=1:1,                       % 左右页边距之比，1:1 为左右页边距相等
    bottom=2.5cm,                           % 底部空白（下右边距）
    headheight=0.6cm,                       % 页眉高度
    headsep=0.8cm,                          % 页眉距上部正文文本高度
    footskip=0.9cm,                         % 页脚底部距底部正文文本的距离
    marginparsep=0.2cm,                     % marginparsep, marginparwidth 可以不设定
    marginparwidth=1.5cm,                   % 设定旁注
    nomarginpar,                            %
    dvipdfm                                 % 必须设定此driver，以利用dvipdfm生成正确的纸张大小
    ]{geometry}

\ifjludebug
    \geometry{showframe,verbose}            % 测试用：显示layout 边框
    %%% 测试用: 评估页面设置的效果
    \RequirePackage{layouts}                % 打印当前页面格式的宏包
    \RequirePackage{layout}                 % 另一个打印当前页面格式的宏包
\fi

\RequirePackage{ifpdf}                      % 用于判断是否为pdf[la]tex运行的宏包

%% 包含图形的宏包
%\ifpdf                                      % we're running pdf[la]tex.
%    \RequirePackage[pdftex]{graphicx}
%    \RequirePackage{cmap}                   % pdf[la]tex编译时，可以生成可复制、粘贴的中文PDF文档
%    \RequirePackage[pdftex]{color}          % 使用各种颜色
%\else
%    \ifpdftex
%        \RequirePackage[pdftex]{graphicx}
%        \RequirePackage{cmap}               % pdf[la]tex编译时，可以生成可复制、粘贴的中文PDF文档
%        \RequirePackage[pdftex]{color}      % 使用各种颜色
%    \else
%        \ifdvi                              % 是否只生成带链接的dvi
%            \RequirePackage[dvipdf]{graphicx}
%            \RequirePackage[dvipdf]{color}
%        \else                               % 默认使用dvimpdfm[x]编译
%            \RequirePackage[dvipdfm]{graphicx}
%            \RequirePackage[dvipdfm]{color}
%        \fi
%    \fi
%\fi

\RequirePackage{graphicx}
\RequirePackage{color}

\RequirePackage{calc}                       % 长度可以用+ - * / 进行计算
\RequirePackage{indentfirst}                % 章节后首段首行缩进
\RequirePackage{fancyhdr}                   % 页眉页脚方便设定
\RequirePackage{fancybox}                   % 生成包含文字的盒子

%%% 重设章节标题和目录的宏包，

\RequirePackage{titlesec}
\RequirePackage{titletoc}

%%% 排印中文和数学符号，公式，表格

\RequirePackage{amsmath}                    %
\RequirePackage{amsfonts}                   %
\RequirePackage{amssymb}                    % AMS 公式、字体、符号
\RequirePackage{txfonts}                    % 希腊字母
\RequirePackage{stmaryrd}                   % 特殊符号

\RequirePackage{array}                      % 增强表格的功能
\RequirePackage{multirow}                   % 使用Multirow宏包，使得表格可以合并多个row格
\RequirePackage{longtable}                  % 支持跨页的表格。
%\RequirePackage{arydshln}                   % 分块矩阵和tabular 画虚线，挺好用
                                            % 避免宏包 hyperref 和 arydshln 不兼容带来的目录链接失效的问题。
                                            %\def\temp{\relax}
                                            %\let\temp\addcontentsline
                                            %\gdef\addcontentsline{\phantomsection\temp}

\RequirePackage{CJK}[2005/08/11]            % 中文排版支持
\RequirePackage{CJKnumb}[2005/08/11]        % 中文数字表示

\ifchinesepunct
    \RequirePackage{CJKpunct}[2005/08/11]
\fi

%\RequirePackage[superscript]{cite}                          % 支持引用的宏包
%\RequirePackage[sort&compress,numbers,super]{natbib}  % 支持引用缩写的宏包
%\RequirePackage[sort&compress,numbers]{natbib}
%\RequirePackage{hypernat}

% 引用等等的超链接
\ifpdf
    \RequirePackage[pdftex,unicode]{hyperref}
    \RequirePackage{cmap}
\else
    \ifpdftex
        \RequirePackage[pdftex,unicode]{hyperref}
    \else
        \ifdvi                              % 是否只生成带链接的dvi
            \RequirePackage[dvipdf,unicode]{hyperref}           % 考虑到yap可以正反向搜索，dvipdf使yap中的链接有效。
        \else                               % 默认使用dvimpdfm[x]编译
            \RequirePackage[dvipdfm,unicode]{hyperref}          %dvi-->pdf 生成书签
            \AtBeginDvi{\special{pdf:tounicode GBK-EUC-UCS2}}   % GBK -> Unicode
        \fi
    \fi
\fi

\ifjludebug
    \hypersetup{%
        CJKbookmarks=true,%
        bookmarksnumbered=true,%
        bookmarksopen=true,%
        breaklinks=true,%
        colorlinks=true,%
        pdfborder={0 0 1},%
        citecolor=blue,%
        linkcolor=red,%
        anchorcolor=green,%
        urlcolor=blue,%
    }
\else
    \ifjluelectronic
        \hypersetup{%
            CJKbookmarks=true,%
            bookmarksnumbered=true,%
%            bookmarksopen=true,%
            breaklinks=true,%
            colorlinks=true,%
            pdfborder={0 0 0},%
            citecolor=blue,%
            linkcolor=red,%
            anchorcolor=green,%
            urlcolor=blue,%
        }
    \else
        \hypersetup{%
            CJKbookmarks=true,%
            bookmarksnumbered=true,%
%            bookmarksopen=true,%
            breaklinks=true,%
            colorlinks=false,
            pdfborder={0 0 0},
        }
    \fi
\fi

%%% 定义字体
\iflinux        % 我的Linux系统下只有宋体和楷体
   \newcommand*{\songti}{\CJKfamily{gbsn}} % 宋体
   \newcommand*{\heiti}{\CJKfamily{gbsn}}   % 黑体
   \newcommand*{\kaishu}{\CJKfamily{kai}}  % 楷书
   \newcommand*{\fangsong}{\CJKfamily{gbsn}} % 仿宋
   \newcommand*{\lishu}{\CJKfamily{gbsn}}    % 隶书
   \newcommand*{\youyuan}{\CJKfamily{gbsn}} % 幼圆
\else
    \newcommand*{\songti}{\CJKfamily{song}} % 宋体
    \newcommand*{\heiti}{\CJKfamily{hei}}   % 黑体
    \newcommand*{\kaishu}{\CJKfamily{kai}}  % 楷书
    \newcommand*{\fangsong}{\CJKfamily{fs}} % 仿宋
    \newcommand*{\lishu}{\CJKfamily{li}}    % 隶书
    \newcommand*{\youyuan}{\CJKfamily{you}} % 幼圆
\fi

%%% 定义字号(字号大小值摘自ctex.sty)，按照经验\baselinskip一般设定为字体大小的1.2倍
% bp: big point, 1bp = (1/72)in = 0.353mm
% pc：
% mu:

\newcommand{\chuhao}{\fontsize{42.16}{50.59}\selectfont}    % 初号字    42bp
\newcommand{\xiaochu}{\fontsize{36.14}{43.36}\selectfont}   % 小初号    36bp
\newcommand{\yihao}{\fontsize{26.10}{31.32}\selectfont}     % 一号字    26bp
\newcommand{\xiaoyi}{\fontsize{24.09}{28.91}\selectfont}    % 小一号    24bp
\newcommand{\erhao}{\fontsize{22.08}{26.50}\selectfont}     % 二号字    22bp
\newcommand{\xiaoer}{\fontsize{18.07}{21.68}\selectfont}    % 小二号    18bp
\newcommand{\sanhao}{\fontsize{16.06}{19.27}\selectfont}    % 三号字    16bp
\newcommand{\xiaosan}{\fontsize{15.06}{18.07}\selectfont}   % 小三号   15bp
\newcommand{\sihao}{\fontsize{14.05}{16.86}\selectfont}     % 四号字   14bp
\newcommand{\xiaosi}{\fontsize{12.05}{14.45}\selectfont}    % 小四号   12bp
\newcommand{\wuhao}{\fontsize{10.54}{12.65}\selectfont}     % 五号字 10.5bp
\newcommand{\xiaowu}{\fontsize{9.03}{10.84}\selectfont}     % 小五号    9bp
\newcommand{\liuhao}{\fontsize{7.53}{9.03}\selectfont}      % 六号字  7.5bp
\newcommand{\xiaoliu}{\fontsize{6.52}{7.83}\selectfont}     % 小六号  6.5bp
\newcommand{\qihao}{\fontsize{5.52}{6.62}\selectfont}       % 七号字  5.5bp
\newcommand{\bahao}{\fontsize{5.02}{6.02}\selectfont}       % 八号字    5bp


%%% 声明数学字号，参考ctex.sty
% \DeclareMathSizes{<t-size>}{<mt-size>}{<s-size>}{<ss-size>} :
% declares that <mt-size> is the (main) math text size, <s-size> is the
% `script' size and <ss-size> the `scriptscript' size to be used in math
% when <t-size> is the current text size.
\DeclareMathSizes{5.02}{5.02}{5}{5}
\DeclareMathSizes{5.52}{5.52}{5}{5}
\DeclareMathSizes{6.52}{6.52}{5}{5}
\DeclareMathSizes{7.53}{7.53}{5}{5}
\DeclareMathSizes{9.03}{9.03}{6}{5}
\DeclareMathSizes{10.54}{10.54}{7}{5}
\DeclareMathSizes{12.05}{12.05}{8}{6}
\DeclareMathSizes{14.05}{14.05}{10.54}{7.53}
\DeclareMathSizes{15.06}{15.06}{12.05}{9.03}
\DeclareMathSizes{16.06}{16.06}{14.05}{10.54}
\DeclareMathSizes{18.07}{18.07}{15.06}{12.05}
\DeclareMathSizes{22.08}{22.08}{16.06}{14.05}
\DeclareMathSizes{24.09}{24.09}{18.07}{15.06}
\DeclareMathSizes{26.10}{26.10}{22.08}{16.06}
\DeclareMathSizes{36.14}{36.14}{24.09}{18.07}
\DeclareMathSizes{42.16}{42.16}{26.10}{22.08}

\newcommand{\defaultfont}{
    \renewcommand{\baselinestretch}{1.37}   % 默认行距为1.37倍，必须放在字体改变命令以前以有效,eg. \small
    \small\normalsize
    \xiaosi
    \songti}

% 初始化中文环境

%\def\jlu@beginCJK{\begin{CJK*}{GBK}{song}}
%\def\jlu@endCJK{\clearpage\end{CJK*}}
\def\jlu@beginCJK{\begin{CJK*}{UTF8}{song}}
\def\jlu@endCJK{\clearpage\end{CJK*}}
\AtBeginDocument{\jlu@beginCJK} \AtEndDocument{\jlu@endCJK}

\AtBeginDocument{\makeatletter\InputIfFileExists{jlumastersthesis.cfg}{}{%
  \PackageError{jlumastersthesis}{%
    Can't find jlumastersthesis}{%
    There will be unexpected errors if you continue.}}
  \makeatother}

\AtBeginDocument{\CJKtilde \CJKindent}

% 设定段落间距: \parskip + \baselineskip
%\setlength{\parskip}{0.75ex plus .2ex minus .5ex}

\endinput
%%
%% end of file `jluthesis.sty'
